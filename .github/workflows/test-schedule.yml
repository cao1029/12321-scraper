name: schedule-test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "SCHEDULE FIRED AT $(date -u)"
          echo "This proves GitHub schedule works for this repo"